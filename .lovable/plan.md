## Plan: switch the app to project `lidbfkytoajumnhwlcry`

### Why a plan and not edits

The three things you asked me to do directly (repoint psql, regenerate `types.ts`, edit `client.ts` / `.env` to a different project) are all blocked in this environment:

- The sandbox `PG*` connection is pinned to the currently linked Cloud project (`uvxzygypxccibioctkxy`). I have no credentials for `lidbfkytoajumnhwlcry`.
- `src/integrations/supabase/types.ts` and `src/integrations/supabase/client.ts` are auto-generated by Lovable Cloud from the linked project. I'm not allowed to hand-edit them, and a hand-edit would be overwritten on the next regeneration anyway.
- `.env` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PROJECT_ID`, `VITE_SUPABASE_PUBLISHABLE_KEY`) is also managed by the Cloud link.

So the switch has to happen at the link layer, not in code.

### Step 1 — you relink the Lovable project (user action)

In this Lovable project: open the Cloud / backend panel and connect it to Supabase project `lidbfkytoajumnhwlcry` instead of `uvxzygypxccibioctkxy`. Once you do that, Lovable will automatically:

- rewrite `.env` with the new URL, project id, and publishable key
- regenerate `src/integrations/supabase/client.ts` against the new project
- regenerate `src/integrations/supabase/types.ts` from the new schema (so it will include `universes`, `stories.universe_id`, and the `universes_with_counts` view)
- repoint my sandbox `PG*` vars at the new project on the next turn

I won't touch any of those files myself.

### Step 2 — I verify (read-only, after relink)

Once you confirm the relink is done, in a follow-up turn I'll:

1. Run `psql -c "select current_user;"` and confirm the username now ends in `.lidbfkytoajumnhwlcry`.
2. Run the three queries you wanted and show raw output:
   - `\d public.universes`
   - `select count(*) from public.universes;`
   - `select * from public.universes_with_counts;`
3. Read the regenerated `src/integrations/supabase/types.ts` and confirm it contains `universes`, `stories.universe_id`, and `universes_with_counts`.
4. Read the regenerated `.env` and `src/integrations/supabase/client.ts` and confirm both show `lidbfkytoajumnhwlcry`.

No schema changes to either project. No edits to `types.ts`, `client.ts`, or `.env` by me. No changes to the existing front-end code (`StoryCard`, `HappyPlace`, `fetchUniverses`) — once types regenerate, the existing code starts resolving against real tables.

### If relinking isn't an option

Tell me, and we'll pick one of: (a) recreate the `universes` schema in the currently linked project via migration, or (b) remove the universes / Story Worlds code from the front end. Both are last-resorts; relinking is the correct fix.
